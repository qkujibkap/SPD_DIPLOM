# VKinder — Бот для поиска знакомств во ВКонтакте

Этот проект представляет собой чат-бота для платформы ВКонтакте, который подбирает пользователей для знакомств на основе данных самого пользователя (возраст, пол, город), показывает карточки людей с фото, а также позволяет сохранять понравившихся в избранное.

---

## 📌 Возможности бота

- Получение информации о пользователе (возраст, пол, город)
- Поиск подходящих людей через VK API
- Отправка карточки с:
  - именем и фамилией
  - ссылкой на профиль
  - тремя топовыми фотографиями по лайкам
- Команды бота:
  - **Следующий** — подобрать нового человека
  - **В избранное** — сохранить человека в favorites.json
  - **Показать избранных** — вывести список избранных
  - **Стоп** — остановить бота

---

# 📁 Структура проекта

```
project/
│── bot.py               # Основной файл бота
│── vk_client.py         # Работа с VK API
│── favorites.py         # Работа с избранным (favorites.json)
│── config.py            # Настройки и токены
│── favorites.json       # Хранилище избранных пользователей
│── README.md            # Описание проекта
```

---

# 🗂 Схема файла favorites.json

Файл предназначен для хранения списка избранных пользователей.

Каждая запись имеет структуру:

```
{
  "id": <int>,                     # ID пользователя ВК
  "first_name": "<str>",           # Имя
  "last_name": "<str>",            # Фамилия
  "profile_url": "<str>",          # Ссылка на профиль
  "photos": [                      # Список вложений фото
    "photo123_456",
    "photo123_789",
    "photo123_101112"
  ],
  "_added_at": "<ISO-8601>",       # Время добавления
}
```

### Поля:

1. **id**  
   Уникальный идентификатор пользователя ВК.

2. **first_name**, **last_name**  
   Используется для отображения имени в избранных.

3. **profile_url**  
   Удобная ссылка на страницу.

4. **photos**  
   Список ID фотографий формата `"photo{owner_id}_{photo_id}"`.

5. **_added_at**  
   Дата и время добавления в ISO‑формате.

---

# ⚙ config.py

Файл не содержит токенов по соображениям безопасности.  
Содержит следующие настройки:

```
TOKEN_GROUP     # Токен группы ВКонтакте
GROUP_ID        # ID группы
TOKEN_USER      # User Token — используется для поиска людей
SEARCH_PARAMS   # Параметры поиска (базовые)
FAVORITES_FILE  # Путь к файлу favorites.json
```

---

# 🧩 favorites.py

Модуль для работы с JSON-файлом:

- загрузка данных (`load_favorites`)
- сохранение (`save_favorites`)
- добавление записи (`add_to_favorites`)
- получение списка избранных (`get_favorites`)

---

# 🔌 vk_client.py

Обёртка над VK API:

- `search_users()` — поиск пользователей по параметрам
- `get_top_photos()` — получение топ‑3 фото по лайкам

Больше не содержит логики favorites (вынесена в отдельный модуль).

---

# 🤖 bot.py

Основной механизм работы:

- обработка команд пользователя
- формирование карточки
- фильтрация поиска по полу, возрасту, городу
- добавление в избранное

---

# ▶ Запуск

1. Установить зависимости:

```
pip install -r requirements.txt
```

2. Заполнить config.py:

```
TOKEN_GROUP = "..."
TOKEN_USER = "..."
GROUP_ID = 123456
SEARCH_PARAMS = {
    "count": 50,
    "country": 1
}
```

3. Запустить бота:

```
python bot.py
```

---

